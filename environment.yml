name: vtp
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - numpy
  - matplotlib
  - scipy
  - pytorch
  - torchvision
  - tqdm
  # waymo converter
  - pandas
  - pyarrow
  - pillow
  - pip
  - pip:
      - ultralytics
      - black
      - -e .
