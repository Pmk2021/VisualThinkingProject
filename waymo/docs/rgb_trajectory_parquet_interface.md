# Generated RGB Trajectory Parquet Interface

This file is generated from the converter implementation.

```bash
python scripts/generate_rgb_trajectory_schema_doc.py --converter waymo/scripts/build_waymo_rgb_trajectory_dataset.py
```

Do not edit this file by hand. Update the converter or the generator, then regenerate it.

## Source

- Converter: `waymo/scripts/build_waymo_rgb_trajectory_dataset.py`
- Extraction method: `write_parquet(...)` calls plus constant keys in row dictionaries.

## Tables

### images.parquet

One row per standalone RGB camera image.

- Output collection: `all_images`
- Builder functions: `build_images_table`

| Column | Inferred Type | Note |
| --- | --- | --- |
| `split` | `string` | Dataset split name. |
| `image_id` | `string` | Unique image key. |
| `scene_id` | `string` | Waymo scene or segment id. |
| `frame_timestamp_micros` | `int64` | Frame timestamp in microseconds. |
| `camera_name` | `int64` | Waymo camera enum id. |
| `camera_name_text` | `string` | Human-readable camera name. |
| `image_jpeg` | `binary` | JPEG-encoded RGB image bytes. |
| `image_format` | `string` | Image encoding label. |
| `image_width` | `int64` | Decoded image width in pixels. |
| `image_height` | `int64` | Decoded image height in pixels. |
| `visible_trajectory_ids` | `list<string>` | Trajectory ids visible in this image. |
| `num_visible_trajectories` | `int64` | Number of visible trajectory ids. |

### ego_poses.parquet

One row per ego/vehicle pose timestamp.

- Output collection: `all_ego_poses`
- Builder functions: `build_ego_poses_table`, `add_finite_difference_ego_velocity`

| Column | Inferred Type | Note |
| --- | --- | --- |
| `split` | `string` | Dataset split name. |
| `scene_id` | `string` | Waymo scene or segment id. |
| `frame_timestamp_micros` | `int64` | Frame timestamp in microseconds. |
| `ego_pose_transform` | `list<float64>` | 4x4 row-major world_from_vehicle transform. |
| `ego_x` | `float64` | Ego x position from vehicle pose. |
| `ego_y` | `float64` | Ego y position from vehicle pose. |
| `ego_z` | `float64` | Ego z position from vehicle pose. |
| `ego_roll` | `float64` | Ego roll from the vehicle pose rotation matrix. |
| `ego_pitch` | `float64` | Ego pitch from the vehicle pose rotation matrix. |
| `ego_yaw` | `float64` | Ego yaw/heading from the vehicle pose rotation matrix. |
| `ego_velocity_x` | `float64` | Preferred ego linear velocity component. |
| `ego_velocity_y` | `float64` | Preferred ego linear velocity component. |
| `ego_velocity_z` | `float64` | Preferred ego linear velocity component. |
| `ego_angular_velocity_x` | `float64` | Ego angular velocity component. |
| `ego_angular_velocity_y` | `float64` | Ego angular velocity component. |
| `ego_angular_velocity_z` | `float64` | Ego angular velocity component. |
| `ego_velocity_source` | `string` | Source for preferred ego linear velocity. |
| `ego_fd_velocity_x` | `float64` | Finite-difference ego velocity component derived from pose translation. |
| `ego_fd_velocity_y` | `float64` | Finite-difference ego velocity component derived from pose translation. |
| `ego_fd_velocity_z` | `float64` | Finite-difference ego velocity component derived from pose translation. |

### trajectories.parquet

One row per scene/object 3D trajectory.

- Output collection: `all_trajectories`
- Builder functions: `build_trajectories_table`

| Column | Inferred Type | Note |
| --- | --- | --- |
| `split` | `string` | Dataset split name. |
| `scene_id` | `string` | Waymo scene or segment id. |
| `trajectory_id` | `string` | Object trajectory id within a scene. |
| `trajectory_row_id` | `string` | Unique trajectory row key. |
| `object_type` | `int64` | Waymo object type enum. |
| `num_steps` | `int64` | Number of trajectory timesteps. |
| `timestamps_micros` | `list<int64>` | Trajectory timestamps in microseconds. |
| `x` | `list<float64>` | Object x positions over time. |
| `y` | `list<float64>` | Object y positions over time. |
| `z` | `list<float64>` | Object z positions over time. |
| `length` | `list<float64>` | Object box lengths over time. |
| `width` | `list<float64>` | Object box widths over time. |
| `height` | `list<float64>` | Object box heights over time. |
| `heading` | `list<float64>` | Object headings over time. |
| `velocity_x` | `list<float64>` | Object velocity component. |
| `velocity_y` | `list<float64>` | Object velocity component. |
| `velocity_z` | `list<float64>` | Object velocity component. |

### image_trajectories.parquet

Visible object links and 2D projected boxes per image.

- Output collection: `all_links`
- Builder functions: `build_image_trajectory_table`

| Column | Inferred Type | Note |
| --- | --- | --- |
| `split` | `string` | Dataset split name. |
| `image_id` | `string` | Unique image key. |
| `scene_id` | `string` | Waymo scene or segment id. |
| `frame_timestamp_micros` | `int64` | Frame timestamp in microseconds. |
| `camera_name` | `int64` | Waymo camera enum id. |
| `camera_name_text` | `string` | Human-readable camera name. |
| `trajectory_id` | `string` | Object trajectory id within a scene. |
| `trajectory_row_id` | `string` | Unique trajectory row key. |
| `object_type` | `int64` | Waymo object type enum. |
| `bbox_center_x` | `float64` | Projected 2D box center x in image pixels. |
| `bbox_center_y` | `float64` | Projected 2D box center y in image pixels. |
| `bbox_width` | `float64` | Projected 2D box width in pixels. |
| `bbox_height` | `float64` | Projected 2D box height in pixels. |
| `bbox_x1` | `float64` | Projected 2D box left coordinate in pixels. |
| `bbox_y1` | `float64` | Projected 2D box top coordinate in pixels. |
| `bbox_x2` | `float64` | Projected 2D box right coordinate in pixels. |
| `bbox_y2` | `float64` | Projected 2D box bottom coordinate in pixels. |

### prediction_targets.parquet

WOMD tracks_to_predict rows with observed history and optional future labels.

- Output collection: `all_prediction_targets`
- Builder functions: `build_prediction_targets`, `prediction_targets_from_scenario`

| Column | Inferred Type | Note |
| --- | --- | --- |
| `split` | `string` | Dataset split name. |
| `scene_id` | `string` | Waymo scene or segment id. |
| `trajectory_id` | `string` | Object trajectory id within a scene. |
| `trajectory_row_id` | `string` | Unique trajectory row key. |
| `track_index` | `int64` | Track index in the WOMD scenario. |
| `track_id` | `int64` | WOMD track id. |
| `rank` | `int64` | Rank/order in tracks_to_predict. |
| `difficulty` | `int64` | WOMD prediction target difficulty enum. |
| `difficulty_name` | `string` | Human-readable difficulty label. |
| `object_type` | `int64` | Waymo object type enum. |
| `object_type_name` | `string` | Human-readable object type label. |
| `current_time_index` | `int64` | WOMD current timestep index. |
| `observed_timestamps_seconds` | `list<float64>` | Observed history array from WOMD current/past timesteps. |
| `observed_valid` | `list<bool>` | Observed history array from WOMD current/past timesteps. |
| `observed_x` | `list<float64>` | Observed history array from WOMD current/past timesteps. |
| `observed_y` | `list<float64>` | Observed history array from WOMD current/past timesteps. |
| `observed_z` | `list<float64>` | Observed history array from WOMD current/past timesteps. |
| `observed_heading` | `list<float64>` | Observed history array from WOMD current/past timesteps. |
| `observed_velocity_x` | `list<float64>` | Observed history array from WOMD current/past timesteps. |
| `observed_velocity_y` | `list<float64>` | Observed history array from WOMD current/past timesteps. |
| `future_timestamps_seconds` | `list<float64>` | Future label array from WOMD timesteps after current_time_index, when present. |
| `future_valid` | `list<bool>` | Future label array from WOMD timesteps after current_time_index, when present. |
| `future_x` | `list<float64>` | Future label array from WOMD timesteps after current_time_index, when present. |
| `future_y` | `list<float64>` | Future label array from WOMD timesteps after current_time_index, when present. |
| `future_z` | `list<float64>` | Future label array from WOMD timesteps after current_time_index, when present. |
| `future_heading` | `list<float64>` | Future label array from WOMD timesteps after current_time_index, when present. |
| `future_velocity_x` | `list<float64>` | Future label array from WOMD timesteps after current_time_index, when present. |
| `future_velocity_y` | `list<float64>` | Future label array from WOMD timesteps after current_time_index, when present. |
| `has_future_gt` | `bool` | Whether future labels were present in the source scenario. |

## Join Hints

- `images.image_id` -> `image_trajectories.image_id`
- `images.(split, scene_id, frame_timestamp_micros)` -> `ego_poses.(split, scene_id, frame_timestamp_micros)`
- `trajectories.trajectory_row_id` -> `image_trajectories.trajectory_row_id`

## Type Inference Notes

Types are inferred statically from the converter code and column naming conventions.
Use `pyarrow.parquet.read_schema(...)` on a concrete output file when you need the exact physical Arrow type for a particular run.
